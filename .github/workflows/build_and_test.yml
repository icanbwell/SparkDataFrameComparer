name: Build and Test

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build_and_test:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    permissions:
      contents: read
      # required so aws-actions/configure-aws-credentials can request a GitHub
      # OIDC token to assume the services-account role below
      id-token: write

    steps:
      - name: Create directory and set permissions
        run: |
          sudo mkdir -p /home/runner/work/${{ github.repository }}
          sudo chown -R runner:runner /home/runner/work/${{ github.repository }}
          sudo chmod 777 /home/runner/work/${{ github.repository }}

      - name: Create reports output folder
        run: mkdir -p /home/runner/work/${{ github.repository }}/reports

      # Checks-out your repository
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # Both Dockerfiles now pull their base image from the services account's
      # private ECR (CIE-8032), so docker must be logged in there before any build:
      #   spark.Dockerfile      -> helix.spark:3.5.5.0-slim
      #   pre-commit.Dockerfile -> helix.spark:3.5.5.0-precommit-slim
      #
      # PREREQUISITE, NOT YET PROVISIONED: unlike helix.pipelines -- which runs this
      # on the self-hosted `main` runner and inherits that runner's instance role --
      # this repo is PUBLIC and runs on ubuntu-latest, so it has no AWS identity.
      # A human must add a GitHub OIDC trust policy for
      # repo:icanbwell/SparkDataFrameComparer to the role below (or provision a
      # dedicated read-only ECR role and set AWS_SERVICES_ECR_ROLE_ARN as a repo
      # variable).  Switching to a self-hosted runner is NOT an acceptable
      # alternative for a public repo: fork pull requests would then execute
      # untrusted code on b.well infrastructure.
      # Note also that `pull_request` runs from forks receive no OIDC token, so
      # external-contributor PRs cannot build these images at all.
      - name: AWS Credentials (services account, for private ECR base images)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: us-east-1
          role-to-assume: ${{ vars.AWS_SERVICES_ECR_ROLE_ARN || 'arn:aws:iam::856965016623:role/services-k8s-service-account-github-actions-runner' }}
          role-session-name: ${{ github.run_id }}-services
          role-duration-seconds: 3600

      - name: Login to Amazon ECR (services)
        uses: aws-actions/amazon-ecr-login@v2
        with:
          skip-logout: true

      - name: pre-commit
        run: make run-pre-commit

      # setup and install everything and run tests
      # https://docs.pytest.org/en/8.2.x/reference/reference.html#confval-junit_logging
      - name: up
        run: make up
      - name: tests
        run: |
          docker compose run --rm --name sdc_tests \
            -v /home/runner/work/${{ github.repository }}/reports:/reports \
            dev \
            pytest \
            -o junit_logging=all -o junit_log_passing_tests=false \
            --tb=long \
            --durations=10 \
            --junitxml=/reports/pytest_report.xml > /home/runner/work/${{ github.repository }}/reports/pytest_output.log

      - name: list pytest report
        if: ${{ always() }}
        run: |
          ls -la /home/runner/work/${{ github.repository }}/reports
          cat /home/runner/work/${{ github.repository }}/reports/pytest_report.xml

      - name: Upload test output
        # Use always() to always run this step to publish test results when there are test failures
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: pytest-output
          path: /home/runner/work/${{ github.repository }}/reports/pytest_output.log

      - name: Upload test report
        # Use always() to always run this step to publish test results when there are test failures
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: pytest-report
          path: /home/runner/work/${{ github.repository }}/reports/pytest_report.xml

      - name: Surface failing tests
        if: always()
        uses: pmeier/pytest-results-action@fdc7f18d9934e38aca411ca9557e6577bd25ca9c # v0.9.0 https://github.com/pmeier/pytest-results-action
        with:
          # A list of JUnit XML files, directories containing the former, and wildcard
          # patterns to process.
          # See @actions/glob for supported patterns.
          path: /home/runner/work/${{ github.repository }}/reports/*.xml

          # (Optional) Add a summary of the results at the top of the report
          summary: true

          # (Optional) Select which results should be included in the report.
          # Follows the same syntax as `pytest -r`
          display-options: fEX

          # (Optional) Fail the workflow if no JUnit XML was found.
          fail-on-empty: true

          # (Optional) Title of the test results section in the workflow summary
          title: Test results

      - name: Publish test results
        uses: dorny/test-reporter@3eeb9fc888e82e8be2fb356bbeec2750231672bc  # v1 https://github.com/dorny/test-reporter
        if: ${{ always() }}
        with:
          name: Pytest Summary
          path: /home/runner/work/${{ github.repository }}/reports/pytest_report.xml
          reporter: java-junit        # Format of test results
          fail-on-error: false        # Do not change the build status if errors are found
          list-tests: 'failed'       # List failed tests in the summary
